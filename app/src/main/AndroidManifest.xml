<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wordwolfgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WordWolfGame">
        <activity android:name=".VoteResultActivity"></activity>
        <activity android:name=".GameEndActivity" />
        <activity android:name=".VoteActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".MemberWordConfirmation" />
        <activity android:name=".MemberNameConfirmation" />
        <activity android:name=".MemberSettingActivity" />
        <activity android:name=".GameSettingActivity" />
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>